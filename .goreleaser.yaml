# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: initrule

builds:
  - id: initrule
    main: .
    binary: initrule
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
      - "386"
      - arm
    goarm:
      - "7"
    ignore:
      # macOS dropped 32-bit long ago
      - goos: darwin
        goarch: "386"
      - goos: darwin
        goarch: arm
      # Windows 32-bit ARM is effectively unused
      - goos: windows
        goarch: arm

# https://goreleaser.com/customization/upx/
# darwin unsupported (Gatekeeper/Ventura+); skip if upx missing from PATH.
upx:
  - enabled: true
    ids: [initrule]
    goos: [linux, windows]
    compress: best

archives:
  - id: initrule
    formats: [tar.gz]
    files:
      - LICENSE
      - README.md
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ .Version }}-next"

changelog:
  use: github
  sort: asc
  abbrev: -1
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Bug fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Performance
      regexp: '^.*?perf(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: Refactor
      regexp: '^.*?refactor(\([[:word:]]+\))??!?:.+$'
      order: 3
    - title: Build
      regexp: '^.*?(build|ci)(\([[:word:]]+\))??!?:.+$'
      order: 4
    - title: Documentation
      regexp: '^.*?docs(\([[:word:]]+\))??!?:.+$'
      order: 5
    - title: Others
      order: 999
  filters:
    exclude:
      - "^test:"
      - "^chore:"
      - "^style:"
      - "^revert:"
      - "^Merge pull request"
      - "^Merge branch"

# Attach initrule.json to the GitHub release via CI (see release.yml).
# scoops generates the file; skip_upload skips scoop-bucket push (scoop-mew syncs the release asset).
# https://goreleaser.com/customization/publish/scoop/
scoops:
  - name: initrule
    url_template: "https://github.com/mewisme/initrule/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    homepage: "https://github.com/mewisme/initrule"
    description: "CLI to install Cursor rule files (.cursor/rules/*.mdc) into a project"
    license: MIT
    goamd64: v1
    skip_upload: true
    repository:
      owner: mewisme
      name: scoop-mew
      branch: main

# Attach initrule.rb to the GitHub release via CI (see release.yml).
# homebrew_casks generates the file; skip_upload skips tap push (homebrew-tools syncs the release asset).
# https://goreleaser.com/customization/publish/homebrew_casks/
homebrew_casks:
  - name: initrule
    ids:
      - initrule
    binaries:
      - initrule
    homepage: "https://github.com/mewisme/initrule"
    description: "CLI to install Cursor rule files (.cursor/rules/*.mdc) into a project"
    directory: Casks
    skip_upload: true
    hooks:
      post:
        # Unsigned binary — clear quarantine so macOS Gatekeeper does not block it.
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/initrule"]
          end
    repository:
      owner: mewisme
      name: homebrew-tools
      branch: main
